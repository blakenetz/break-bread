import React from 'react'
import {
	NavigatorIOS,

} from 'react-native'

import styles from '../../assets/styles'

const MainFooter = ({}) => {
	return (
		<NavigatorIOS>

		</NavigatorIOS>
	)
}

export default MainFooter